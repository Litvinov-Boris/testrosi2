from django.urls import path
from . import views

urlpatterns = [

    #path('<str:user_uid>/orders', views.get_orders),
    #path('<str:user_uid>/<str:order_uid>', views.get_order),
    #path('<str:user_uid>/<str:order_uid>/warranty', views.req_warranty),
    #path('<str:user_uid>/purchase', views.do_purchase),
    #path('<str:user_uid>/<str:order_uid>/refund', views.req_refund)
]